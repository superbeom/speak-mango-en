# useSWR 전략 및 가이드라인

## 1. useSWR이란?

**useSWR**은 Vercel 팀(Next.js 개발팀)에서 개발한 데이터 가져오기(Data Fetching)를 위한 React Hook 라이브러리입니다.

- **이름 유래**: HTTP 캐시 무효화 전략인 **Stale-While-Revalidate**에서 따왔습니다.
- **핵심 개념**: "캐시된 데이터(Stale)를 먼저 보여주고, 백그라운드에서 데이터를 재검증(Revalidate)하여 최신화한다"는 것입니다.

### 왜 사용하는가? (사용 사례)

서버에서 데이터를 가져오는 Server Components(`async/await`)와 달리, useSWR은 **클라이언트 사이드**에서 데이터를 가져올 때 주로 사용합니다:

1.  **실시간 데이터**: 주식 차트, 알림, 실시간 댓글 등 (주기적 갱신 필요).
2.  **사용자 반응형 데이터**: 좋아요 수, 장바구니, 로그인 사용자 정보 (상호작용에 따라 변화).
3.  **빠른 네비게이션 (Optimistic UI)**: 페이지 이동 시 로딩 없이 즉시 이전 데이터를 보여주고 싶을 때.
4.  **중복 제거 (Deduplication)**: 여러 컴포넌트에서 동일한 API를 호출할 때 서버 부하를 줄이기 위해.

---

## 2. Speak Mango 도입 시 이점

### A. 빠른 네비게이션 (UX)

- 사용자가 상세 페이지에서 `표현 리스트`로 돌아올 때, SWR은 캐시된 리스트를 **즉시** 보여줍니다.
- 이로 인해 "로딩 스피너 깜빡임"이 사라지고 앱이 쾌적하게 느껴집니다.

### B. 자동 갱신 (Automatic Revalidation)

- **Focus Revalidation**: 브라우저 탭을 나갔다가 다시 클릭했을 때 데이터를 갱신합니다.
- **Reconnect Revalidation**: 네트워크가 끊겼다가 다시 연결됐을 때 데이터를 갱신합니다.

### C. 중복 요청 제거 (Deduplication)

- 만약 `Header`의 유저 정보와 `Sidebar`의 유저 정보 등 여러 곳에서 동일한 데이터를 필요로 한다면, 전역 상태 관리 도구 없이도 `useSWR('/api/user')`를 호출하면 **실제 네트워크 요청은 딱 1번만** 발생합니다.

### D. 코드 간소화

- 복잡한 `useEffect`, `useState`, `isLoading` 로직을 단 한 줄로 대체할 수 있습니다.

---

## 3. 기본 사용법

```tsx
import useSWR from "swr";

// 1. Fetcher 함수 정의 (fetch 또는 axios 사용)
const fetcher = (url: string) => fetch(url).then((res) => res.json());

function Profile() {
  // 2. 사용: useSWR(키, fetcher)
  const { data, error, isLoading } = useSWR("/api/user", fetcher);

  if (error) return <div>Failed to load</div>;
  if (isLoading) return <div>Loading...</div>;

  // 3. 렌더링
  return <div>Hello, {data.name}!</div>;
}
```

---

## 4. Speak Mango 적용 계획

현재 코드베이스를 분석하여 `useSWR`이 가장 효과적인 부분을 선별했습니다.

### ✅ [Implemented] 무한 스크롤 (`usePaginatedList`)

현재 `hooks/usePaginatedList.ts`는 페이지 상태(`items`, `page`)를 수동으로 관리하고, 뒤로가기 복원을 위해 `ExpressionContext`라는 별도의 커스텀 Context를 사용하여 캐싱하고 있습니다.

**제안: `useSWRInfinite` 도입**

`useSWRInfinite`로 전환하면 이 로직을 대폭 단순화할 수 있습니다:

1.  **수동 캐싱 제거**: SWR 내장 전역 캐시가 데이터 보존을 자동으로 처리합니다.
2.  **복잡한 상태 제거**: `items` 배열 합치기 등의 로직을 SWR이 대신합니다.
3.  **자동 동기화**: 사용자가 다른 곳에서 표현을 수정하면, 키 매칭을 통해 리스트도 자동으로 업데이트될 수 있습니다.

**구현 예시:**

```tsx
import useSWRInfinite from "swr/infinite";

const getKey = (pageIndex, previousPageData) => {
  if (previousPageData && !previousPageData.length) return null; // 데이터 끝 도달
  return `/api/expressions?page=${pageIndex + 1}&limit=12`; // API 엔드포인트 키
};

export function useExpressionInfinite() {
  const { data, size, setSize, isLoading } = useSWRInfinite(getKey, fetcher);

  // 데이터 평탄화 (2차원 배열 -> 1차원 배열)
  const expressions = data ? data.flat() : [];
  const isLoadingMore =
    isLoading || (size > 0 && data && typeof data[size - 1] === "undefined");
  const isEmpty = data?.[0]?.length === 0;
  const isReachingEnd = isEmpty || (data && data[data.length - 1]?.length < 12);

  return { expressions, loadMore: () => setSize(size + 1), isReachingEnd };
}
```

> **구현 완료 (2026-01-21)**: `hooks/usePaginatedList.ts` 리팩토링 및 `ExpressionContext` 최적화(items 제거)를 통해 적용 완료되었습니다. 필터 키 직렬화(`JSON.stringify`)를 통해 안정성을 확보했습니다.

### 🔜 추후 적용 후보

1.  **퀴즈 랭킹 / 통계**:
    - "실시간 리더보드"나 "전체 사용자 통계" 기능 추가 시 `refreshInterval` 옵션과 함께 사용하면 완벽합니다.
2.  **사용자 학습 진도**:
    - 퀴즈 결과 제출 후, 페이지 새로고침 없이 "나의 학습 현황" 대시보드를 즉시 업데이트하고 싶을 때 `mutate`(낙관적 업데이트)를 사용하면 매우 빠릿한 경험을 제공할 수 있습니다.
